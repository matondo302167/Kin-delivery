version: '3.8'

services:
  app:
    build:
      context: .
      target: server
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - HOST=0.0.0.0
    restart: unless-stopped

